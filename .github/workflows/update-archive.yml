name: Sync Archive from Google Drive

on:
  schedule:
    - cron: '0 3 * * *'  # Каждый день в 8:00 по ЕКБ (UTC+5)
  workflow_dispatch:

jobs:
  sync:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        run: npm init -y && npm install node-fetch@2

      - name: Sync archive files from Google Drive
        env:
          ARCHIVE_FILES: |
            # 2025-10 fullData.json:1ONAxssw3MN_tS-ScPKeGDaIp3ZmPJrdT
            # standard fullData.json:1Ua_KETb1fVu3PhR6SmxYkWjOOApaZDc5
            #  staff fullData.json:1cbHKB7vGBslK52H7EJBT3jhG0yHFrpC8
            # 2025-09 fullData.json:1qAMfjSXpoeExA-sjTHMZ7_26TBx8ULbO
            2025-11 fullData.json:1tl89YvvdxAF7GZasSZoPxn6zaZuGTuCt
            # Добавляйте новые месяцы по мере появления
        run: |
          mkdir -p archive
          node <<'EOF'
          const fs = require('fs');
          const fetch = require('node-fetch');

          const archiveList = process.env.ARCHIVE_FILES
            .split('\n')
            .map(line => line.trim())
            .filter(line => line && !line.startsWith('#'))
            .map(line => {
              const [name, id] = line.split(':');
              return { name: name.trim(), id: id.trim() };
            });

          async function downloadFile(fileName, fileId) {
            const url = `https://drive.google.com/uc?export=download&id=${fileId}`;
            try {
              const response = await fetch(url);
              if (!response.ok) throw new Error(`HTTP ${response.status}`);
              const text = await response.text();
              fs.writeFileSync(`archive/${fileName}`, text, 'utf8');
              console.log(`✅ ${fileName}`);
            } catch (err) {
              console.error(`❌ Не удалось скачать ${fileName}: ${err.message}`);
            }
          }

          async function main() {
            for (const file of archiveList) {
              await downloadFile(file.name, file.id);
              await new Promise(r => setTimeout(r, 1000));
            }
          }

          main().catch(console.error);
          EOF

      - name: Commit and push
        run: |
          git config --global user.name 'github-actions[bot]'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'
          
          git add archive/
          
          if ! git diff --staged --quiet; then
            git commit -m "🔄 Обновление архивов [$(date +'%Y-%m-%d')]"
            git push
            echo "✅ Изменения отправлены"
          else
            echo "ℹ️ Нет изменений"
          fi
