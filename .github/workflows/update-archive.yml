name: Update archive.json from Google Sheets

on:
  schedule:
    - cron: '0 9 * * *'  # 14:00 по ЕКБ (UTC+5)
  workflow_dispatch:

jobs:
  update:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Generate JSON files
        run: node scripts/generate-Archive.mjs

      - name: Commit and push
        run: |
          git config --global user.name 'github-actions[bot]'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'
          
          # Добавляем ВСЕ сгенерированные файлы
          git add fullData.json archive/
          
          # Проверяем, есть ли изменения
          if ! git diff --staged --quiet; then
            git commit -m "🔄 Обновление данных [$(date +'%Y-%m-%d %H:%M')]"
            git push
            echo "✅ Изменения успешно отправлены"
          else
            echo "ℹ️ Нет изменений — коммит не требуется"
          fi
