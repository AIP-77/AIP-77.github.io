name: Update and Archive Monthly Data

on:
  schedule:
    - cron: '00 15 * * *'
  workflow_dispatch:

jobs:
  update:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      # 1. Обновляем текущие данные → current.json
      - name: Generate current.json
        run: node scripts/generate-current.mjs

      # 2. Архивация (только 1-го числа)
      - name: Archive previous month if needed
        run: |
          DAY=$(date -u +%d)
          if [ "$DAY" = "01" ]; then
            echo "Сегодня 1-е число — архивируем прошлый месяц..."
            PREV_MONTH=$(date -u -d "yesterday" +%Y-%m)
            mkdir -p archive
            cp current.json archive/${PREV_MONTH}.json
            git add archive/${PREV_MONTH}.json
            git config --global user.name 'github-actions[bot]'
            git config --global user.email 'github-actions[bot]@users.noreply.github.com'
            git commit -m "📦 Архив: ${PREV_MONTH}.json"
            git push
          else
            echo "Не 1-е число — архивация не требуется."
          fi

      # 3. Опционально: обновить data.json как копию current.json (если Mini App читает data.json)
      - name: Update data.json (as alias to current.json)
        run: |
          cp current.json data.json
          git add data.json
          if ! git diff --quiet -- data.json; then
            git config --global user.name 'github-actions[bot]'
            git config --global user.email 'github-actions[bot]@users.noreply.github.com'
            git commit -m "🔄 Обновление данных [$(date +'%Y-%m-%d %H:%M')]"
            git push
          fi
